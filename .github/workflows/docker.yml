name: Docker 

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  check-tools:

    runs-on: ubuntu-latest
    container: terraform:1.3.5

    steps:
      - run: docker version
      - run: terraform version
      - run: go version
      - run: id 
